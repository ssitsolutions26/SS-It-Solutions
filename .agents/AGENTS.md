# Custom Agent Rules for SS IT Solutions

- **Deployment Protocol:** DO NOT automatically push changes to the remote repository. Always allow the user to review the changes and run the project locally on `localhost` first. The user will manually handle `git push` when they are ready.
