<Window x:Class="SolarQuotationBillingSystem.Views.BillDeleteConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Delete Bill Confirmation" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="Cambria">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="WARNING" Foreground="#D32F2F" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="You are about to permanently delete this bill." FontWeight="SemiBold" TextWrapping="Wrap" HorizontalAlignment="Center"/>
            <TextBlock Text="This action cannot be undone." Foreground="#D32F2F" FontWeight="Bold" Margin="0,5,0,20" HorizontalAlignment="Center"/>

            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,15" Background="#F9F9F9">
                <StackPanel>
                    <TextBlock Text="Bill Details" FontWeight="Bold" Margin="0,0,0,5"/>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <TextBlock Text="Invoice No:" Width="90" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding InvoiceNo}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <TextBlock Text="Date:" Width="90" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding BillDate, StringFormat=dd-MMM-yyyy}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <TextBlock Text="Customer:" Width="90" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding CustomerName}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <TextBlock Text="Total Amount:" Width="90" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TotalAmount, StringFormat=₹ {0:N2}}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <TextBlock Text="Delete Remark *" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding DeleteRemark, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5,0,10" Height="60" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

            <TextBlock Text="Admin Password *" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <PasswordBox x:Name="txtPassword" Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="0,5,0,10" PasswordChanged="txtPassword_PasswordChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Delete" Command="{Binding DeleteCommand}" Background="#D32F2F" BorderBrush="#D32F2F" Foreground="White" Width="100" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
