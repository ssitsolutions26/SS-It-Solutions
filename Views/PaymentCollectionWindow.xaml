<Window x:Class="SolarQuotationBillingSystem.Views.PaymentCollectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Collect Payment" Height="600" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="Cambria">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Collect Payment" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Invoice Number:" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding InvoiceNo, Mode=OneWay}" IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5,0,10"/>

            <TextBlock Text="Customer Name:" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding CustomerName, Mode=OneWay}" IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5,0,10"/>

            <TextBlock Text="Bill Amount:" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding BillAmount, Mode=OneWay, StringFormat=₹ {0:N2}}" IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5,0,10"/>

            <TextBlock Text="Remaining Balance:" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding RemainingBalance, Mode=OneWay, StringFormat=₹ {0:N2}}" IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5,0,10" Foreground="Red" FontWeight="Bold"/>

            <TextBlock Text="Paid Amount:" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding PaidAmount, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5,0,10"/>

            <TextBlock Text="Payment Mode:" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <ComboBox SelectedItem="{Binding PaymentMode}" ItemsSource="{Binding PaymentModes}" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,5,0,10"/>

            <TextBlock Text="Reference Number (UPI/Bank/Cheque):" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding ReferenceNo}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5,0,10"/>

            <TextBlock Text="Payment Date:" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <DatePicker SelectedDate="{Binding PaymentDate}" Style="{StaticResource MaterialDesignOutlinedDatePicker}" Margin="0,5,0,10"/>

            <TextBlock Text="Remarks:" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding Remarks}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5,0,10" AcceptsReturn="True" Height="60"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save Payment" Command="{Binding SavePaymentCommand}" Background="#4CAF50" BorderBrush="#4CAF50" Foreground="White" Width="120" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CloseCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
