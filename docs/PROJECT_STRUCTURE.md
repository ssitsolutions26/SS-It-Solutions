# Project Structure

This document outlines the organization of the codebase.

`	ext
/
â”œâ”€â”€ index.html           # Main entry point
â”œâ”€â”€ css/                 # Cascading Style Sheets
â”‚   â””â”€â”€ style.css        # Main application styles
â”œâ”€â”€ js/                  # JavaScript logic
â”‚   â””â”€â”€ main.js          # Core application scripts
â”œâ”€â”€ assets/              # Static media assets (Legacy)
â”œâ”€â”€ images/              # Modern image assets
â”œâ”€â”€ icons/               # SVG/PNG icons
â”œâ”€â”€ fonts/               # Custom web fonts
â”œâ”€â”€ docs/                # Project documentation
â”œâ”€â”€ components/          # Reusable UI HTML fragments (Future)
â””â”€â”€ .github/workflows/   # CI/CD pipeline definitions
`
"@

# 7. docs/INSTALLATION.md
 = @"
# Installation Guide

This project consists of static HTML, CSS, and JS files. There is no complex build step required.

## Prerequisites
- Any modern web browser (Chrome, Firefox, Edge, Safari)
- Optional: VS Code with Live Server extension for active development.

## Setup
1. Clone the repository:
   `ash
   git clone https://github.com/ssitsolutions26/ssitsolutions26.github.io.git
   `
2. Open the directory in your preferred code editor.
3. Open index.html in your browser to view the site.